@model IEnumerable<classlar.Models.Lot>

@{
    ViewBag.Title = "Lot";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "CreateLot")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.LotType)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LotQuantity)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.LotType)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LotQuantity)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.LotID }) |
            @Html.ActionLink("Details", "Details", new { id=item.LotID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.LotID })
        </td>
    </tr>
}

</table>
